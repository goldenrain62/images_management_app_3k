DATABASE_URL=mysql://username:password@localhost:3306/database_name

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_secret_key_here

# Base path for the app (leave empty for local dev, set to /admin for production)
BASE_PATH=

# Public image base URL (leave as-is for local dev, set to production image URL for deployment)
IMAGE_BASE_URL=http://localhost:3000/uploads
